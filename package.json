{
  "name": "specdest-corporate-site",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "dev": "vite",
    "build:client": "vite build",
    "build:server": "vite build --ssr src/entry-server.tsx --outDir dist-server",
    "build": "tsc --noEmit && npm run build:client && npm run build:server && node scripts/prerender.mjs",
    "preview": "vite preview",
    "seo:audit": "node scripts/seo-audit.mjs",
    "deploy": "npm run build && pnpm dlx firebase-tools deploy --only hosting --project specdest-lp",
    "deploy:hosting": "pnpm dlx firebase-tools deploy --only hosting --project specdest-lp"
  },
  "dependencies": {
    "react": "18.2.0",
    "react-dom": "18.2.0",
    "react-router-dom": "^6.4.5"
  },
  "devDependencies": {
    "@types/react": "18.0.26",
    "@types/react-dom": "18.0.9",
    "@vitejs/plugin-react": "3.0.0",
    "typescript": "4.9.3",
    "vite": "4.0.0"
  }
}
